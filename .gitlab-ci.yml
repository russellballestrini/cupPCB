stages:
  - deploy

deploy:
  stage: deploy
  tags: ["proxy.uncloseai.com"]
  script:
    - echo "{\"version\":\"$CI_COMMIT_TAG\"}" > version.json
    - sudo /usr/local/bin/deploy-www.sh cuppcb .
  rules:
    - if: '$CI_COMMIT_TAG =~ /^\d+\.\d+\.\d+$/'
